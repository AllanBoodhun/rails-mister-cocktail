<h1> <%= @cocktail.name %> </h1>
<% @cocktail.doses.each do |dose| %>
<p><%= dose.description %></p>
<p><%= link_to 'delete',
          dose_path(dose),
          method: :delete,
          data: { confirm: 'Are you sure?'} %>
    </li></p>
<%end  %>

<% @cocktail.ingredients.each do |ingredient| %>
<p><%= ingredient.name %></p>
<%end  %>



<%= link_to 'Add ingredients with dose', new_cocktail_dose_path(@cocktail) %>
<%= link_to 'Back', cocktails_path %> 